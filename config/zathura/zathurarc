# zathurarc

# settings

# key bindings

# map  <A-Button1> inverse_search

set synctex true 

# set synctex-editor-command "nvr --servername /tmp/nvimVt07zN/0 -c %{line} %{input}"
set synctex-editor-command "var=$(cat ~/servername.txt);nvr --servername var --remote-silent +%{line} %{input}" 

